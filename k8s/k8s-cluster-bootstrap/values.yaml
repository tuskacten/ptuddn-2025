#################################################################################################
# Set the credentials for Google Cloud Config Connector
configConnector:
  googleServiceAccount: "k8s-service-account@gke-k8-cluster.iam.gserviceaccount.com" #FIXME Update the value if new project

## Replace ONLY the projectId < k8s-service-account@PROJECT_ID.iam.gserviceaccount.com > 
## Service Account is configured by Terraform
#################################################################################################


certManager:
  name: cert-manager

chaosMonkey:
  name: kube-monkey


octant:
  name: octant
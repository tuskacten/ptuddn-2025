# Eshop namespace
kind: Namespace
apiVersion: v1
metadata:
  name: eshop-staging
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    cnrm.cloud.google.com/project-id: gke-k8-cluster   #FIXME Update the value if new project
  labels:
    istio-injection: enabled